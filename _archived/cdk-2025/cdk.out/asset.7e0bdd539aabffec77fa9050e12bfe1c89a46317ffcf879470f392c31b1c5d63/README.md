# Flowency Invite Lambda

**⚠️ NOT BNDY INFRASTRUCTURE**

This Lambda function is part of the **Flowency** project, NOT the BNDY platform.

## Deployment Info

- **AWS Function Name**: `flowency-invite-lambda`
- **Purpose**: Flowency invite management (separate from BNDY)
- **CloudFormation Stack**: NOT part of `bndy-serverless-api` stack
- **Repository**: Stored here for convenience, but logically separate

## Why It's Here

This function is stored in the `bndy-serverless-api` repository but is **not deployed as part of the BNDY platform infrastructure**.

## Important Notes

- Do NOT include in BNDY deployment scripts
- Do NOT include in BNDY production baselines
- Managed separately from BNDY serverless API
- Not referenced in BNDY Platform Bible